  /** @type {import('next').NextConfig} */
  const nextConfig = {
    experimental: {
      viewTransition: true,
    },
    /* config options here */
    reactCompiler: true,
  };

  export default nextConfig;
